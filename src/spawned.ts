// const { spawn } = require('child_process');

// const pythonScriptPath = 'path/to/your/python/script.py';
// const filePath = 'path/to/the/uploaded/file';

// // Call the Python script and pass it the path to the uploaded file
// const pythonProcess = spawn('python', [pythonScriptPath, filePath]);

// pythonProcess.stdout.on('data', (data) => {
//   console.log(`Python script stdout: ${data}`);
// });

// pythonProcess.stderr.on('data', (data) => {
//   console.error(`Python script stderr: ${data}`);
// });

// pythonProcess.on('close', (code) => {
//   console.log(`Python script exited with code ${code}`);
// });
